import express from  'express';

const app = express.Router();

// app.get('/:boardId/tasks');

// app.post('/:boardId/tasks');

// app.get('/:boardId/tasks/:taskId');

// app.patch('/:boardId/tasks/:taskId');

// app.delete('/:boardId/tasks/:taskId');

// app.post('/:boardId/tasks/:taskId/assignees')

// app.delete('/:boardId/tasks/:taskId/assignees/:userId')

// app.patch('/:boardId/tasks/:taskId/status')

export default app;